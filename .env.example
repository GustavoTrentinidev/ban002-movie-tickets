DATABASE_URL="mongodb://localhost:27017/movie_tickets?replicaSet=rs0&directConnection=true"

# Secret key used to sign session JWTs (see core/infrastructure/security/jose-session-token-service.ts).
# Generate one with: openssl rand -base64 32
SESSION_SECRET=""
